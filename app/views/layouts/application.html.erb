<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? "#{yield(:title)} | Einstein" : "Einstein" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="container">
      <% if user_signed_in? %>
        <%= render 'layouts/navbar' %>
      <% end %>
      <% flash.each do |type, msg| %>
        <%= alert_box msg, context: bootstrap_flash_class(type), dismissible: true %>
      <% end %>
      <div class="row">
        <div class="col-xs-12">
          <%= yield %>
          <%= javascript_include_tag "application" %>
          <%= render 'layouts/footer' %>
        </div>
      </div>
    </div>
  </body>
</html>
